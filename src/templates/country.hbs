
<h2 class="country-title">{{name}}</h2>
<div class="country-container">
    <p>Capital:{{capital}}</p>
    <p>Population:{{population}}</p>
    <p>Languages:{{languages}}</p>
    <ul>
        {{#each languages}}
        <li>{{this.name}}</li>
        {{/each}}
    </ul>
</div>
<div class="flag-container">
    <img class="flag-img" src="{{flag}}" alt="country-flag">
</div>